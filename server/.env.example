# Example environment variables for the server

# PORT for the Express server (default: 4000)
PORT=4000

# MongoDB connection URI (optional for persistence)
# MONGO_URI=mongodb://localhost:27017/jargonaut
# or MongoDB Atlas:
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/jargonaut?retryWrites=true&w=majority
